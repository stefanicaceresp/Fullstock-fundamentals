<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Iniciar Sesión | FullStock</title>

    <!-- CSS global -->
    <link rel="stylesheet" href="/styles/index.css" />
  </head>

  <body>
    <!-- ================= HEADER ================= -->
    <%- include('partials/header', { variant: 'header-auth' }) %>


    <!-- ================= LOGIN ================= -->
    <main class="login-page">
      <div class="login-container">
        <h1>Inicia sesión en tu cuenta</h1>

        <!-- Inputs estáticos, sin validación -->
        <div class="login-form">
          <div class="form-group">
            <label for="email">Correo electrónico</label>
            <input
              type="email"
              id="email"
              name="email"
              placeholder="testino@mail.com"
            />
          </div>

          <div class="form-group">
            <label for="password">Contraseña</label>
            <input
              type="password"
              id="password"
              name="password"
              placeholder="*******"
            />
          </div>

          <!-- Botón que redirige a index.ejs -->
          <a href="/" class="btn-primary"> Iniciar sesión </a>
        </div>

        <p class="signup-link">
          ¿Aún no tienes cuenta?
          <a href="/signup">Crea una cuenta</a>
        </p>
      </div>
    </main>

    <!-- ================= FOOTER ================= -->
    <%- include('partials/footer') %>
  </body>
</html>
